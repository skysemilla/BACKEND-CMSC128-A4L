create database fsr_ms;

create table FACULTY(
	emp_id varchar(10),
	emp_type varchar(20),
	department varchar (10),
	college varchar(20)

);

create table FACULTY_NAME(
	f_name varchar(10),
	m_namevarchar(10),
	l_name varchar (10)

);